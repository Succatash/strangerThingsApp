import {useEffect, useRef} from 'react';

const useDidMountEffect = (func, deps, {test}) => {
	const didMount = useRef(false);

	useEffect(() => {
		if (didMount.current) {
			func();
			console.log(test);
		} else didMount.current = true;
	}, [deps]);
};

export default useDidMountEffect;
